module.exports.execute = async (
  client,
  message,
  locale,
  embed,
  tools,
  knex,
  props,
  data
) => {};

module.exports.props = {
  name: "racing",
  perms: "general",
  alias: ["경마", "경주"],
  args: [{}]
};
